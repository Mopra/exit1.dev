{
  "name": "exit1-mcp",
  "version": "0.2.0",
  "description": "MCP server for Exit1 uptime monitoring — set up monitors, configure alerts, and query uptime from AI assistants",
  "keywords": ["mcp", "exit1", "uptime", "monitoring", "model-context-protocol", "observability", "alerting"],
  "license": "MIT",
  "author": "Exit1",
  "repository": {
    "type": "git",
    "url": "https://github.com/exit1-dev/exit1-mcp"
  },
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "exit1-mcp": "./bin/exit1-mcp.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
